{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "concurrently \"npm run start:database\" \"wait-on http://localhost:5173 && npm run start:handler\"",
    "start:database": "cd database && npm run prod",
    "start:handler": "cd handler && npm run prod",
    "dev": "concurrently \"npm run dev:database\" \"wait-on http://localhost:5173 && npm run dev:handler\"",
    "dev:database": "cd database && npm run dev",
    "dev:handler": "cd handler && npm run dev",
    "install:all": "npm install && concurrently \"npm run install:database\" \"npm run install:handler\"",
    "install:database": "cd database && npm install",
    "install:handler": "cd handler && npm install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^9.1.2",
    "wait-on": "^8.0.3"
  }
}
