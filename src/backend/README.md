## How to start

Run `npm run install:all` in the backend folder (has to be done only once)

`npm run start` starts 3 databases and the handler

`npm run dev` start 1 database and the handler with nodemon
